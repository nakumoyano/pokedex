import { ColorTypePipe } from './color-type.pipe';

describe('ColorTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorTypePipe();
    expect(pipe).toBeTruthy();
  });
});
