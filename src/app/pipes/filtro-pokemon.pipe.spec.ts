import { FiltroPokemonPipe } from './filtro-pokemon.pipe';

describe('FiltroPokemonPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPokemonPipe();
    expect(pipe).toBeTruthy();
  });
});
